# ─────────────────────────────────────────────────────────────────────────────
# edp-test-v17 [DEV] — Feature Workspace Configuration
#
# Auto-generated by: scaffold_workspace.py
# Generated at: 2026-03-05 19:03 UTC
#
# Feature workspaces are ephemeral — created per feature branch,
# destroyed after PR merge. Items come from Fabric Git Sync.
# ─────────────────────────────────────────────────────────────────────────────

workspace:
  name: ${PROJECT_PREFIX}
  display_name: ${PROJECT_PREFIX}
  description: "edp-test-v17 [DEV] feature branch workspace"
  capacity_id: ${FABRIC_CAPACITY_ID}
  domain: ${FABRIC_DOMAIN_NAME}
  git_repo: ${GIT_REPO_URL}
  git_branch: main
  git_directory: /e2e_test_scaffold

environments:
  dev:
    workspace:
      name: ${PROJECT_PREFIX}
      capacity_id: ${FABRIC_CAPACITY_ID}

# Folder structure — same as base workspace
folders:
  - "000 Orchestrate"
  - "100 Ingest"
  - "200 Store"
  - "300 Prepare"
  - "400 Model"
  - "500 Visualize"
  - "999 Libraries"
  - "Archive"

# No Fabric items — content is managed through Fabric Git Sync
lakehouses: []
notebooks: []
resources: []

# Mandatory governance principals
principals:
  - id: "${AZURE_CLIENT_ID}"
    type: ServicePrincipal
    role: Admin
    description: Automation SP — required for CI/CD deployments

  - id: "${ADDITIONAL_ADMIN_PRINCIPAL_ID}"
    type: Group
    role: Admin
    description: Mandatory governance admin group

  - id: "${ADDITIONAL_CONTRIBUTOR_PRINCIPAL_ID}"
    type: Group
    role: Contributor
    description: Mandatory governance contributor group

  # TODO: Add project-specific principals
  # - id: "${E2E_TEST_SCAFFOLD_ADMIN_ID}"
  #   type: Group
  #   role: Admin
  #   description: "edp-test-v17 [DEV] project admins"

